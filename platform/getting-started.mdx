---
title: "Getting started"
description: "Sign in, find your way around the Worlds dashboard, and create your first world."
---

Plato is where you build a realistic slice of real work — the apps, the files, the data — and set an AI agent loose on a task inside it, then let a grader check whether the AI got it right. This guide is about **using the platform itself**: the screens, the buttons, and the flow from a blank world to a submitted task.

## Sign in

We'll send you a **special access link** that signs you in as a **creator**. Open that link, complete sign-in, and you'll land on the **Worlds** dashboard — your home base.

<Note>
Use the link we sent you — that's what grants your creator access. If you sign in without it and don't see the creator tools, reach out and we'll get you set up.
</Note>

## The Worlds dashboard

Everything starts from a **world**: a workspace where you combine **simulators** (the apps, like Gmail or Google Sheets) and **datasets**, and build a task on top of them. Each world holds **exactly one task**.

The dashboard lists all your worlds as cards. Each card shows the world's **name**, its **status** (for example *Running* or *Stopped*), its size and creation date, and a row of quick actions on the right: **tag**, **duplicate**, **share**, and **archive**.

### Finding your worlds

A few controls at the top help you navigate as your list grows:

- **Status tabs** — **All · In progress · In review · Rejected · Approved**. Every world moves through these as you build and submit it, so the tabs double as your progress tracker.
- **My worlds / All users** — toggle between just your worlds and everyone's.
- **Filter by name**, a **Tags** filter, a **sort** dropdown (e.g. *Newest first*), and **Show archived**.

### The top-bar tools

Three buttons at the top open platform-wide tools you'll use occasionally:

<CardGroup cols={3}>
  <Card title="Global memory">
    Shared notes and context available across your worlds.
  </Card>
  <Card title="Task taxonomy">
    The catalog of occupations and workflows tasks are organized under.
  </Card>
  <Card title="Agent profiles">
    The configured agents (and judges) that can run against your tasks.
  </Card>
</CardGroup>

## Create a world

<Steps>
  <Step title="Choose the world type">
    Use the type dropdown (it defaults to **Scenario world**) at the top of the dashboard.
  </Step>
  <Step title="Name it">
    Type a short, descriptive name in the **New world name…** field. Use one clear name per world — you'll build a single task inside it.
  </Step>
  <Step title="Click Create">
    Hit **+ Create**. The world spins up and opens in the editor, ready to build.
  </Step>
</Steps>

<Note>
**One task per world.** Every world holds a single task — there's no way to add more, and nothing to manage. If you want to build another task on the **same seed data**, **clone** the world and change the task in the copy (see [Reusing seed data for a new task](/platform/building-a-world-and-task#reusing-seed-data-for-a-new-task)).
</Note>

Next: [Building a world and task](/platform/building-a-world-and-task).
